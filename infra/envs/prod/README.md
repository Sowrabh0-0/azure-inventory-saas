# Production

This repository is optimized for dev VM testing per the current requirement. For production, keep the container boundary but add managed PostgreSQL, managed Redis, TLS automation, backups, private networking, image scanning, and centralized monitoring.

